import { CalendarProjectionDirective } from './projection.directive';

describe('ProjectionDirective', () => {
  it('should create an instance', () => {
    // const directive = new CalendarProjectionDirective();
    // expect(directive).toBeTruthy();
  });
});
